/// An enumeration representing the axis along which matrix operations can be performed.
public enum MatrixAxis {
  /// Indicates operations to be performed along the rows of a matrix.
  case row

  /// Indicates operations to be performed along the columns of a matrix.
  case column
}
